# JARVIS Gateway/Knowledge Workflow Handoff (2026-03-06)

## 当前阶段

- A01~A20: completed
- B01~B05: completed
- 下一步: **B06（人工审核 / Webhook / 模板流程首批）**

## 最新已完成（本次会话）

1. B05 Workflow DAG 执行引擎 MVP 已落地
2. 新增 Workflow API：
   - `POST /api/v1/workflows`
   - `GET /api/v1/workflows`
   - `GET /api/v1/workflows/{workflowId}`
   - `POST /api/v1/workflows/{workflowId}/runs`
   - `GET /api/v1/workflow-runs/{runId}`
3. 执行能力：
   - DAG 校验（单 start / 无环 / 可达性 / 边合法）
   - 条件分支执行
   - 节点 `retry_max` 与 `timeout_ms`
   - `idempotency_key` 幂等重放
4. 冒烟验证通过：
   - `b05_smoke` success
   - `b02_smoke` success
   - `b03_smoke` success
   - `b04_eval` gate pass

## 关键文件

- `knowledge-workflow-service/app/routers/workflow.py`
- `knowledge-workflow-service/app/services/workflow_engine.py`
- `knowledge-workflow-service/app/services/workflow_store.py`
- `knowledge-workflow-service/app/models/schemas.py`
- `knowledge-workflow-service/scripts/b05_smoke.sh`
- `scripts/b05_smoke.sh`
- `docs/B05-WorkflowDAG执行引擎MVP说明.md`

## 下次启动继续指令（给助手）

请读取：

- `docs/HANDOFF-2026-03-06-B05.md`

然后继续：

- 进入 B06，交付人工审核节点、Webhook 节点、首批模板流程与 B06 冒烟脚本；
- 完成后更新 README、交付文档并准备提交。

